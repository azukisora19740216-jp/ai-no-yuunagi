# リポジトリ調査結果

- 調査日: 2026-07-20
- 調査対象: リポジトリルート、隠しファイル、全ファイル一覧、`/docs`
- 状態: 初回調査完了

## 結論

リポジトリは実質的に空です。調査開始時に存在したのは生成済みの `outputs/` と `work/` ディレクトリだけで、アプリケーションコード、パッケージ定義、DBスキーマ、CI、Docker設定、Git管理情報、既存の `AGENTS.md` は確認できませんでした。

依頼で参照必須とされた次の資料も、調査時点では `/docs` 自体が存在せず、未配置でした。

- 利用規約
- プライバシーポリシー
- 禁止品基準
- 寄付団体審査基準
- 配送運用基準
- 運営概念図
- 行政相談用概要
- 想定質問・回答集

このため、本初回文書はユーザーから提示された依頼文のみを一次要件として作成しています。予定資料との整合確認は未実施であり、資料受領後に必ず差分レビューが必要です。

## 現状の技術構成

| 項目             | 現状           | 扱い                                                    |
| ---------------- | -------------- | ------------------------------------------------------- |
| 言語・ランタイム | 未選定・未導入 | TypeScript / Node.js LTSを暫定提案                      |
| Web              | 未導入         | Next.js App Routerを暫定提案                            |
| DB・ORM          | 未導入         | PostgreSQL + Prismaを暫定提案                           |
| 認証             | 未導入         | 実績ある認証ライブラリをPhase 1で比較・ADR化            |
| オブジェクト保存 | 未導入         | S3互換API + ローカルモックを暫定提案                    |
| メール           | 未導入         | 差し替え可能なアダプター + 開発用メール受信UIを暫定提案 |
| テスト           | 未導入         | Vitest、Testing Library、Playwrightを暫定提案           |
| ローカル環境     | 未導入         | Docker Composeを暫定提案                                |
| CI               | 未導入         | GitHub Actionsを暫定提案（ホスティング先は未決）        |

「採用」は実装開始承認後、Phase 1のADRとロックファイル作成をもって確定します。現在はバージョンを固定していません。

## 調査に使用したコマンド

```powershell
Get-Location
rg --files -g '!node_modules' -g '!\.git' | Sort-Object
Get-ChildItem -Force | Select-Object Mode,Length,LastWriteTime,Name
```

## 調査上の制約

- 参照予定資料がないため、制度資料間の矛盾はまだ比較できない。
- 実装がないため、依存関係監査、静的解析、型検査、テストは実行対象がない。
- デプロイ先、運営主体、想定規模、SLA、予算が未定であり、非機能要件の数値は確定できない。

## 次回の資料取込手順

1. 原本を `docs/source/` 等の読取専用扱いの場所へ配置する。
2. 文書名、版、施行・更新日、承認者、原本URLまたは由来を一覧化する。
3. 各要件に出典と優先度を付ける。
4. 本文書群との差分を `requirements-conflicts.md` に追記する。
5. 法務・運用責任者の決定を記録し、必要ならADRを作成する。
